name: TPPets
version: 0.9.1
description: Teleport pets to certain areas of the map
load: POSTWORLD
author: GatheringExp
main: com.maxwellwheeler.plugins.tppets.TPPets
depend: [WorldEdit]
softdepend: [Vault]

commands:
  tpp:
    description: Base command for TPPets
    aliases: [teleportpets]
    usage: "Usage: /tpp [parrots/dogs/cats/protected/lost]"
  generate-tamed-cats:
    description: Testing only
  tp-forward:
    description: Testing only

permissions:
  tppets.*:
    description: Allows teleportation of every pet, and allows teleportation in protected areas.
    default: op
    children:
      tppets.bypassprotection: true
      tppets.tpanywhere: true
      tppets.protected: true
      tppets.lost: true
      tppets.dogs: true
      tppets.cats: true
      tppets.birds: true
  tppets.untameall:
    default: op
    description: Allows user to untame pets that they don't own.
  tppets.bypassprotection:
    default: op
    description: Allows user to damage pets that they don't own.
  tppets.tpanywhere:
    default: op
    description: Allows user to teleport pets in protected regions.
  tppets.protected:
    default: op
    description: Allows user to set cuboid regions where pets are not allowed.
  tppets.lost:
    default: op
    description: Allows user to set cuboid regions where pets are teleported if they are caught in a no-pets area.
  tppets.dogs:
    default: true
    description: Allows teleporting dogs.
  tppets.cats:
    default: true
    description: Allows teleporting cats.
  tppets.birds:
    default: true
    description: Allows teleporting parrots.